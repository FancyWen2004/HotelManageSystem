spring:
  # 配置日期序列化方式
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
    # 配置数据源
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/hotel?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: 123456
  # 配置redis
  data:
    redis:
      host: localhost
      port: 6379
      database: 1
  mail:
    host: smtp.163.com
    username: Korn_Zen2020@163.com
    password: XRkGX9i4vsqz3dGJ
    protocol: smtp
#logging:
#  config: classpath:log4j2.xml
  web:
    resources:
      add-mappings: false
# mybatis-plus配置
mybatis-plus:
  global-config:
    db-config:
      # 更新策略
      update-strategy: ignored
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

springdoc:
  #平展swagger参数,即：不使用对象包裹参数让对象的属性平铺在平展的参数中
  default-flat-param-object: true
#knife4j的增强配置，开启文件上传功能
knife4j:
  enable: true
  setting:
    # 设置knife4j的语言国际化
    language: zh_cn

minio:
  endpoint: http://192.168.10.100:9000
  access-key: minioadmin
  secret-key: minioadmin
  bucket-name: apartment